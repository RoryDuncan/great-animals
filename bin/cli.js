// Generated by CoffeeScript 1.10.0
(function() {
  var animals, i, j, options, pkg, program, ref;

  program = require('commander');

  pkg = require('../package.json');

  animals = require('./index.js');

  program.version(pkg.version).option("-c  --count", "the number of animal uuids to generate.").option("-t  --tokenlength", "The length of the token generated. Pass 0 to not have a token.").parse(process.argv);

  options = {};

  if (program.tokenlength) {
    options.tokenLength = Math.min(48, Math.max(0, program.tokenlength));
  }

  if (program.count) {
    program.count = Math.max(program.count, 0);
    for (i = j = 0, ref = program.count; 0 <= ref ? j < ref : j > ref; i = 0 <= ref ? ++j : --j) {
      process.stdout.write(animals(options) + "\n");
    }
  } else {
    console.log(animals(options));
  }

}).call(this);
